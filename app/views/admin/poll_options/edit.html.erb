<% form_for(@poll_option, :url => admin_poll_poll_option_path(@poll, @poll_option), :html => { :method => :put }) do |f| -%>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p class="form-buttons">
    <%= button t("update") %>
    <%= t("or") %> <%= link_to t("cancel"), admin_poll_poll_options_path %>
  </p>
<% end -%>
